Playground console is `app.js` (plain JS, no gzip eval).
Preview: `npm run dev` on port 8080.
OpenCode mock: `/__opencode` (ships a seeded session for the Session Index).
Session chips persist the active tab, rename on double-click, and refetch remote messages on switch.
Credit Charles @zanneth. MIT.
